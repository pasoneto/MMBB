var querySequence = window.location.search;
const urlSequence = new URLSearchParams(querySequence);

var nextTask = urlParams.get('next')

console.log(nextTask)

//var whereSend = "https://pasoneto.github.io/MMBB/src/batteries/" +  + ".html";
//whichNext()
